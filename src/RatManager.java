package application;

public class RatManager {
    private ArrayList<Rat> ratList = new ArrayList<>;

    public getRat() {

    }

    public addRat(Rat rat) {
        ratList.add()
    }

    public ratActions() {
        //Something like this?
        for (Rat r in ratList) {
            r.ratActions()
        }
    }
}